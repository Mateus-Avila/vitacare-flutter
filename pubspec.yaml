name: vitacare_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.0.0
  cupertino_icons: ^1.0.8
  device_preview_plus: ^2.6.1
  firebase_auth: ^6.0.0
  firebase_core: ^4.0.0
  google_fonts: ^6.3.2
  http: ^1.2.2
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/vitacare_logo.png
